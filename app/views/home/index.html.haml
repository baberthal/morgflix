.container-fluid
  .jumbotron.jumbotron-fluid
    .container
      %h1.display-3.text-center Log In
      %br
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .field
          = f.label :email
          = f.email_field :email, autofocus: true

        .field
          = f.label :password
          = f.password_field :password, autocomplete: "off"

        - if devise_mapping.rememberable?
          .field
            = f.check_box :remember_me
            = f.label :remember_me

        .actions
          = f.submit 'Log In'

      = render "devise/shared/links"
      -# %form
      -#   .form-group.row
      -#     %label.sr-only{for: 'userInputEmail'} Email Address
      -#     %input.form-control#userInputEmail{type: 'email', placeholder: 'Email' }
      -#   .form-group.row
      -#     %label.sr-only{for: 'userInputPassword'} Password
      -#     %input.form-control#userInputPassword{type: 'email', placeholder: 'Password' }
      -#   .form-group.row
      -#     %button.btn.btn-primary.btn-block Go
